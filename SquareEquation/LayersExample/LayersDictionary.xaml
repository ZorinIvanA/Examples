<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="ITemplate" >
        <Border BorderBrush="Orange" BorderThickness="2" Margin="5" CornerRadius="5" Padding="10">
            <StackPanel>
                <TextBlock Text="{Binding Path=ItemName}"></TextBlock>
                <TextBlock Text="{Binding Path=ItemAmount}"></TextBlock>
                <TextBlock Text="{Binding Path=CreationDate}"></TextBlock>
            </StackPanel>
        </Border>

    </DataTemplate>

</ResourceDictionary>